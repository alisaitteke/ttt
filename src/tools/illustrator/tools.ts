import type { ToolDefinition } from '../../core/tool-registry.js';

/**
 * Illustrator MCP tools (not implemented yet).
 * Extend this module when automation for Adobe Illustrator is added.
 */
export function createIllustratorTools(): ToolDefinition[] {
  return [];
}
